name: run integration tests

on:
  push:
    branches:
      - dev

env:
  RC: ""

jobs:
  setReleaseCandidate:
    runs-on:
      - ubuntu-latest
    
    steps:
      - name: set env
        run: ${{ github.workflow }}
       
  getRC:
    runs-on:
      - ubuntu-latest
      
    steps:
      - name: get env
        run: echo "${{ env.RC }}"
